name: Warrant Canary (DISABLED — moved to Cloudflare KV worker cron)

# Canary is now maintained by the Cloudflare Worker cron (every 5 min) writing to KV.
# This workflow is intentionally disabled to prevent hourly commits triggering Netlify deploys.
on:
  workflow_dispatch: {}  # manual only, no schedule

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Warrant canary is handled by Cloudflare Worker — nothing to do here."
